spring:
  config:
    import: "optional:configserver:"
  application:
    name: MY_DATA_SERVICE
  datasource:
    url: jdbc:h2:file:./data-service/data/emp_mgmt_db
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: create


logging:
  level:
    org:
      hibernate:
        SQL: debug
#        type:
#          descriptor:
#            sql: trace

#Spring Cloud ####################

server:
  port: 9001

